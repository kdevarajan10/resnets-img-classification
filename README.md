# resnets-img-classification
To implement the basic building blocks of ResNets and put together these building blocks to implement and train a state-of-the-art neural network for image classification.
